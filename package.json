{
  "name": "opencart-playwright-automation-suite",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright automation framework for OpenCart using Page Object Model.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report reports/html"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
